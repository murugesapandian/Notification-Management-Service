<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <!-- 3.3.13 is the latest version actually published to Maven Central for the
             3.3.x line (verified directly against repo1.maven.org, not just a version-
             checker tool — some sources report newer 3.3.x numbers that are a paid
             vendor's back-ported build, not a real public release). Bumped from 3.3.4
             specifically to pick up fixes for CVE-2025-41249, CVE-2025-22233, and
             CVE-2025-41234, accumulated across the nine patch releases in between; see
             docs/testing-strategy.md "Security review" for the full account of what
             was and wasn't upgraded and why. -->
        <version>3.3.13</version>
        <relativePath/>
    </parent>

    <groupId>com.schwab</groupId>
    <artifactId>notification-management-service</artifactId>
    <version>1.0.0</version>
    <name>notification-management-service</name>
    <description>Notification Management Service - AI-assisted engineering assignment prototype</description>
    <packaging>jar</packaging>

    <properties>
        <java.version>17</java.version>
        <!-- Left at 2.6.0, not bumped: 2.9.0 and (confirmed by actually trying it)
             2.8.17 both reference o.s.web.servlet.resource.LiteWebJarsResourceResolver,
             a class that doesn't exist in the Spring Framework 6.1.x line Spring Boot
             3.3.x manages — springdoc's Spring Boot floor moved up somewhere in the
             2.7/2.8 line. No CVE was found motivating this bump in the first place
             (the one known swagger-ui XSS CVE, affecting webjars:swagger-ui 3.14–3.38,
             was already long behind us at the bundled 5.x version) — see
             docs/testing-strategy.md "Security review" for the full account. -->
        <springdoc.version>2.6.0</springdoc.version>
        <jacoco.version>0.8.12</jacoco.version>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-jpa</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-validation</artifactId>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-actuator</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-core</artifactId>
        </dependency>
        <dependency>
            <groupId>org.flywaydb</groupId>
            <artifactId>flyway-database-postgresql</artifactId>
        </dependency>
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <!-- Pinned above Spring Boot's managed version: 42.7.13 is the latest patch
                 in the same 42.7.x line on Maven Central (verified directly), 42.7.4 was
                 several patches behind. -->
            <version>42.7.13</version>
            <scope>runtime</scope>
        </dependency>
        <!-- Demo/dev database (default profile) — file-based so data survives a restart,
             unlike H2's in-memory default. See docs/architecture-overview.md §2 (DB choice)
             and application.yml. Postgres above remains the production profile. -->
        <dependency>
            <groupId>org.xerial</groupId>
            <artifactId>sqlite-jdbc</artifactId>
            <version>3.53.4.0</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.hibernate.orm</groupId>
            <artifactId>hibernate-community-dialects</artifactId>
            <version>6.5.3.Final</version>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.48</version>
            <optional>true</optional>
        </dependency>
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-starter-webmvc-ui</artifactId>
            <version>${springdoc.version}</version>
        </dependency>
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.awaitility</groupId>
            <artifactId>awaitility</artifactId>
            <version>4.3.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>
        <plugins>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <configuration>
                    <excludes>
                        <exclude>
                            <groupId>org.projectlombok</groupId>
                            <artifactId>lombok</artifactId>
                        </exclude>
                    </excludes>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-failsafe-plugin</artifactId>
                <configuration>
                    <!-- Lets the integration-test JVM record coverage too (see the jacoco
                         prepare-agent-integration execution below), so NotificationApiIT's
                         exercise of the controller/exception-handler layer is actually
                         counted instead of showing as a false 0% gap. -->
                    <argLine>@{failsafeArgLine}</argLine>
                </configuration>
                <executions>
                    <execution>
                        <goals>
                            <goal>integration-test</goal>
                            <goal>verify</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>${jacoco.version}</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals><goal>prepare-agent</goal></goals>
                        <configuration>
                            <destFile>${project.build.directory}/jacoco.exec</destFile>
                        </configuration>
                    </execution>
                    <execution>
                        <id>prepare-agent-integration</id>
                        <phase>pre-integration-test</phase>
                        <goals><goal>prepare-agent-integration</goal></goals>
                        <configuration>
                            <destFile>${project.build.directory}/jacoco.exec</destFile>
                            <propertyName>failsafeArgLine</propertyName>
                        </configuration>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals><goal>report</goal></goals>
                    </execution>
                    <execution>
                        <id>check</id>
                        <phase>verify</phase>
                        <goals><goal>check</goal></goals>
                        <configuration>
                            <rules>
                                <rule>
                                    <element>PACKAGE</element>
                                    <includes>
                                        <include>com.schwab.nms.application*</include>
                                        <include>com.schwab.nms.domain*</include>
                                    </includes>
                                    <limits>
                                        <limit>
                                            <counter>LINE</counter>
                                            <value>COVEREDRATIO</value>
                                            <minimum>0.90</minimum>
                                        </limit>
                                    </limits>
                                </rule>
                            </rules>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
